// var webHost = window.location.protocol + "\/\/" + window.location.host
window.GLOBAL_CONFIG_SETTING = {
  // web服务器地址
  WEB_BASE: window.location.protocol + "://" + window.location.host,
  // 接口路径前缀
  WEB_BASE_API: "http://192.168.50.143:9998",
  // 允许上传的文件类型，* 表示允许全部，
  ACCEPT_FILE_TYPE: "*"
}